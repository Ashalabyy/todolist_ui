import 'package:flutter/material.dart';

class MandatoryTasks extends StatelessWidget {
  static const routeName = '/MandatoryScreen';
  const MandatoryTasks({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mandatory...'),
    );
  }
}
