import 'package:flutter/material.dart';

class PendingTasks extends StatelessWidget {
  static const routeName = '/PendingTaskScreen';
  const PendingTasks({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pending...'),
    );
  }
}
